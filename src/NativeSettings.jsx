import {useEffect,useMemo,useState} from 'react'

const API='/api/finance'
async function req(path,options={}){const r=await fetch(API+path,{credentials:'same-origin',cache:'no-store',...options,headers:{...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={detail:t}}if(!r.ok)throw new Error(typeof d.detail==='string'?d.detail:`HTTP ${r.status}`);return d}
const pretty=s=>String(s||'').replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase())
const PLATFORM_KEYS=new Set(['company_platform','automation_monitoring','automation_zero_touch','reporting_notifications','backup_policy'])
const DESCRIPTIONS={company_platform:'Core Atlaris platform identity and administrative defaults.',automation_monitoring:'Operational monitoring thresholds and refresh policy.',automation_zero_touch:'Default policies applied to new Zero Touch customer onboarding.',reporting_notifications:'Notification and escalation defaults for operational events.',backup_policy:'Backup retention and restore-safety defaults for managed operations.'}
const groupFor=k=>{if(k==='company_platform')return'Platform';if(k==='automation_monitoring')return'Monitoring';if(k==='automation_zero_touch')return'Zero Touch';if(k==='reporting_notifications')return'Notifications';if(k==='backup_policy')return'Backups';if(k==='company')return'Company & Fiscal';if(k==='tax')return'Tax';if(k==='payments')return'Payments';if(k==='approvals'||k.includes('approval'))return'Approvals';if(k.includes('invoice')||k.includes('billing'))return'Billing & Invoicing';if(k==='backup'||k==='backups')return'Finance Backups';if(k.includes('security')||k.includes('session')||k.includes('role'))return'Security';return'Finance Other'}
const scopeFor=k=>PLATFORM_KEYS.has(k)?'Platform':'Finance'
const scopeDescription=s=>s==='Platform'?'Controls that govern the Atlaris administration platform, Zero Touch, monitoring, notifications and operational safety.':'Accounting, billing, tax, payments, approval and Finance-specific operating defaults.'

function Editor({row,onClose,onSaved}){const[draft,setDraft]=useState(structuredClone(row.setting_value||{})),[busy,setBusy]=useState(false),[error,setError]=useState('');async function save(e){e.preventDefault();setBusy(true);setError('');try{await req(`/settings/${encodeURIComponent(row.setting_key)}`,{method:'POST',body:JSON.stringify({value:draft})});onSaved()}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="native-modal-bg"><div className="native-modal"><header><div><span className="eyebrow">{scopeFor(row.setting_key)} · {groupFor(row.setting_key)}</span><h3>{pretty(row.setting_key)}</h3><p>{row.description||DESCRIPTIONS[row.setting_key]||'Atlaris managed configuration policy.'}</p></div><button className="secondary-button" onClick={onClose}>Close</button></header>{error&&<div className="operation-banner error">{error}</div>}<form onSubmit={save} className="native-setting-form">{Object.entries(draft).map(([k,v])=><label key={k}><span>{pretty(k)}</span>{typeof v==='boolean'?<button type="button" className={`native-toggle ${v?'on':''}`} onClick={()=>setDraft(x=>({...x,[k]:!v}))}>{v?'Enabled':'Disabled'}</button>:<input type={typeof v==='number'?'number':'text'} value={v??''} onChange={e=>setDraft(x=>({...x,[k]:typeof v==='number'?Number(e.target.value):e.target.value}))}/>}</label>)}<button className="primary-button" disabled={busy}>{busy?'Saving…':'Save settings'}</button></form></div></div>}

export default function NativeSettings(){
 const[rows,setRows]=useState([]),[editing,setEditing]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(false),[scope,setScope]=useState('All')
 async function load(){setLoading(true);try{const data=await req('/settings');setRows(Array.isArray(data)?data:[]);setError('')}catch(e){setError(e.message)}finally{setLoading(false)}}
 useEffect(()=>{load()},[])
 const scoped=useMemo(()=>scope==='All'?rows:rows.filter(r=>scopeFor(r.setting_key)===scope),[rows,scope])
 const grouped=useMemo(()=>{const order=['Platform','Monitoring','Zero Touch','Notifications','Backups','Company & Fiscal','Tax','Payments','Approvals','Billing & Invoicing','Finance Backups','Security','Finance Other'];const map={};for(const r of scoped){const g=groupFor(r.setting_key);(map[g]||(map[g]=[])).push(r)}return order.filter(x=>map[x]).map(x=>[x,map[x]])},[scoped])
 const counts=useMemo(()=>({Platform:rows.filter(r=>scopeFor(r.setting_key)==='Platform').length,Finance:rows.filter(r=>scopeFor(r.setting_key)==='Finance').length}),[rows])
 return <><div className="section-title"><div><span className="eyebrow">Administration</span><h2>Settings</h2><p>Central policy store with a clear separation between Atlaris platform controls and Finance configuration.</p></div><button className="secondary-button" onClick={load} disabled={loading}>{loading?'Refreshing…':'Refresh'}</button></div><section className="settings-scope-grid"><button className={`settings-scope ${scope==='All'?'active':''}`} onClick={()=>setScope('All')}><span>All policies</span><strong>{rows.length}</strong><small>Complete configuration inventory</small></button><button className={`settings-scope platform ${scope==='Platform'?'active':''}`} onClick={()=>setScope('Platform')}><span>Platform</span><strong>{counts.Platform}</strong><small>{scopeDescription('Platform')}</small></button><button className={`settings-scope finance ${scope==='Finance'?'active':''}`} onClick={()=>setScope('Finance')}><span>Finance</span><strong>{counts.Finance}</strong><small>{scopeDescription('Finance')}</small></button></section><div className="settings-source-note"><strong>Configuration source:</strong> persisted Finance API settings store. Dashboard browser bootstrap is no longer required.</div>{error&&<div className="operation-banner error">{error}</div>}{!loading&&!error&&rows.length===0&&<section className="panel"><div className="empty-state">No settings records are available.</div></section>}{grouped.map(([group,items])=><section className="panel native-settings-group" key={group}><div className="table-header"><div><span className="eyebrow">{scopeFor(items[0].setting_key)} · {group}</span><h3>{group} policies</h3></div><span className="table-note">{items.length} polic{items.length===1?'y':'ies'}</span></div><div className="native-settings-grid">{items.map(r=><article className="subpanel native-setting-card" key={r.setting_key}><div className="native-setting-head"><div><span className={`setting-scope-badge ${scopeFor(r.setting_key).toLowerCase()}`}>{scopeFor(r.setting_key)}</span><span className="eyebrow">{pretty(r.setting_key)}</span></div><h4>{r.description||DESCRIPTIONS[r.setting_key]||'Atlaris managed configuration policy.'}</h4></div><div className="native-values">{Object.entries(r.setting_value||{}).map(([k,v])=><div key={k}><span>{pretty(k)}</span><strong>{typeof v==='boolean'?(v?'Enabled':'Disabled'):String(v)}</strong></div>)}</div><button className="secondary-button" onClick={()=>setEditing(r)}>Edit</button></article>)}</div></section>)}{editing&&<Editor row={editing} onClose={()=>setEditing(null)} onSaved={()=>{setEditing(null);load()}}/>}<style>{`.settings-scope-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin:16px 0}.settings-scope{display:grid;text-align:left;gap:6px;padding:16px;border:1px solid #20364d;border-radius:12px;background:#0b1725;color:#dce8f5;cursor:pointer}.settings-scope.active{border-color:#4978b9;background:#10233a}.settings-scope span{font-size:10px;text-transform:uppercase;letter-spacing:.12em;color:#7d94af}.settings-scope strong{font-size:26px}.settings-scope small{color:#778ca4;line-height:1.45}.settings-source-note{padding:11px 13px;border:1px solid #1c354d;background:#081522;border-radius:10px;color:#7890aa;font-size:10px}.settings-source-note strong{color:#a8bed7}.native-settings-group{margin-top:16px}.native-settings-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.native-setting-card{display:flex;flex-direction:column;gap:14px}.native-setting-head>div{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.setting-scope-badge{padding:4px 7px;border-radius:999px;font-size:8px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.setting-scope-badge.platform{background:#142c49;color:#9fc2ef}.setting-scope-badge.finance{background:#142a22;color:#8ed1b5}.native-setting-card h4{margin:6px 0;color:#c9d6e5}.native-values{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.native-values div{padding:10px;border:1px solid #1d344a;border-radius:9px;background:#081522}.native-values span{display:block;font-size:9px;text-transform:uppercase;color:#7087a1}.native-values strong{display:block;margin-top:4px;font-size:12px;overflow-wrap:anywhere}.native-modal-bg{position:fixed;z-index:1500;inset:0;background:#000b;display:grid;place-items:center;padding:20px}.native-modal{width:min(680px,95vw);max-height:88vh;overflow:auto;border:1px solid #2b4562;background:#0a1625;border-radius:16px;padding:22px;color:#edf4ff}.native-modal>header{display:flex;justify-content:space-between;gap:14px}.native-modal>header p{color:#7d92a9}.native-setting-form{display:grid;gap:10px;margin-top:16px}.native-setting-form label{display:grid;grid-template-columns:minmax(180px,.9fr) 1.1fr;gap:12px;align-items:center;padding:8px 0;border-bottom:1px solid #182b3f}.native-setting-form label>span{font-size:11px;color:#8fa4bc}.native-setting-form input{width:100%;padding:10px;border-radius:8px;border:1px solid #29415a;background:#07131f;color:#fff}.native-toggle{width:max-content!important}.native-toggle.on{border-color:#426da8;background:#17365a}@media(max-width:850px){.settings-scope-grid,.native-settings-grid,.native-values{grid-template-columns:1fr}.native-setting-form label{grid-template-columns:1fr}}`}</style></>
}
